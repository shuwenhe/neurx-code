### This file is automatically generated by Qt Design Studio.
### Do not change

qt6_add_qml_module(${CMAKE_PROJECT_NAME}
    URI "Main"
    VERSION 1.0
    RESOURCE_PREFIX "/qt/qml"
    NO_PLUGIN
    QML_FILES main.qml
)

# Only add subdirectories if they haven't been added yet (avoid duplication with FetchContent)
if(NOT TARGET content)
    add_subdirectory(content)
endif()

if(NOT TARGET NeurXCode)
    add_subdirectory(imports)
endif()

target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE
    content
    NeurXCode
)

if(APPLE)
    # Force-load the static QML resource archive so the generated
    # qInitResources_content_raw_* symbols are always linked.
    target_link_options(${CMAKE_PROJECT_NAME} PRIVATE
        "LINKER:-force_load,$<TARGET_FILE:content>"
    )
endif()
